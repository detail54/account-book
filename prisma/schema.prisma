generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:account.db"
}

model StoreCategory {
  id       Int       @id @default(autoincrement()) @map("store_category_id")
  name     String    @unique
  stores   Store[]
  accounts Account[]
}

model Store {
  id         Int           @id @default(autoincrement()) @map("store_id")
  category   StoreCategory @relation(fields: [categoryId], references: [id], onDelete: Cascade)
  categoryId Int
  name       String        @unique
  accounts   Account[]
}

model Account {
  id         Int           @id @default(autoincrement()) @map("account_id")
  user       User          @relation(fields: [userId], references: [id])
  userId     Int
  regDt      DateTime      @default(now())
  updatedDt  DateTime      @updatedAt
  paymentDt  DateTime
  category   StoreCategory @relation(fields: [categoryId], references: [id], onDelete: Cascade)
  categoryId Int
  store      Store         @relation(fields: [storeId], references: [id], onDelete: Cascade)
  storeId    Int
  amount     Int
  memo       String?
}

model User {
  id       Int       @id @default(autoincrement())
  userName String
  password String
  accounts Account[]
  regDt    DateTime  @default(now())
}
